[build]
  command = "npm run build"
  publish = "dist"

# The following redirect is required for Single Page Applications (SPAs)
# It ensures that client-side routing works correctly when users refresh the page
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
